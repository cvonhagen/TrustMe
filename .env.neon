# TrustMe Password Manager - Neon Database Environment
# Für Entwicklung mit externer Neon PostgreSQL Datenbank

# Backend Configuration
PORT=3030
NODE_ENV=development

# Neon Database Configuration
# Ersetze mit deiner echten Neon Connection String
DATABASE_URL=postgresql://securme_owner:npg_2cUZlf8XzgPy@ep-mute-star-a23xllqn-pooler.eu-central-1.aws.neon.tech/securme?sslmode=require

# JWT Configuration (Development)
JWT_SECRET_KEY=bd22321099622cb9fa7e4268f50934f373d80ced

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Email Configuration (MailHog für Development)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@trustme.local

# Frontend Configuration
VITE_BACKEND_URL=http://localhost:3030/api/v1
VITE_APP_NAME=TrustMe Password Manager (Neon DB)

# Logging
LOG_LEVEL=debug
LOG_FORMAT=pretty

# Development Features
HOT_RELOAD=true
DEBUG_MODE=true
